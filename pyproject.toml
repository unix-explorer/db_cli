[project]
name = "db-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "unix-explorer", email = "mwanzaandrew343@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mysql-connector-python>=26.7.0",
]

[project.scripts]
db-cli = "db_cli:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
